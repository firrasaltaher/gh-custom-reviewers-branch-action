# Branch-specific reviewer requirements
branches:
  main:
    reviewers:
      - firrasaltaher
      - jhaindev
    require_minimum: 2  # Both must approve

  documentation-updates:
    reviewers:
      - jhaindev
    require_minimum: 1  # Only jhaindev must approve
